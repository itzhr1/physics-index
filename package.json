{
  "name": "physics-index",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A GitHub Pages-ready, multi-source physics literature search interface.",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check dist/app.js && node --check dist/js/core.js && node --check dist/js/providers.js && node --check dist/js/subjects.js && node --check worker/src/index.js",
    "serve": "python3 -m http.server 4173 --directory dist"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
