// For GitHub Pages, leave apiBase empty to search Crossref and OpenAlex directly.
// To enable all configured sources, deploy the optional worker and set its URL here.
export const CONFIG = {
  apiBase: '',
  requestTimeoutMs: 14000,
  resultsPerSource: 50,
};
